{% extends 'base.html.twig' %}

{% block title %}{{'user.new'|trans({}, 'ui')}}{% endblock %}

{% block body %}
    <h1>{{'user.create_new'|trans({}, 'ui')}}</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('app_user_index') }}">{{'back_to_list'|trans}}</a>
{% endblock %}
