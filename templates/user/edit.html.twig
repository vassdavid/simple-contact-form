{% extends 'layouts/admin.html.twig' %}

{% block title %}{{'user.edit'|trans({},'ui')}}{% endblock %}

{% block main %}
    <h1>{{'user.edit'|trans({},'ui')}}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_index') }}">{{'back_to_list'|trans}}</a>
{% endblock %}
